{
  "_id": "MWH6Za46Bjii884i",
  "_stats": {
    "compendiumSource": null,
    "coreVersion": "12.343",
    "createdTime": 1752170827742,
    "duplicateSource": "Item.RZJCu7gqGQL3qQkE",
    "systemId": "custom-system-builder",
    "systemVersion": "4.8.5"
  },
  "flags": {
    "custom-system-builder": {
      "version": "4.6.4"
    },
    "fucompanion": {
      "itemMigratorBackup": {
        "category": "skill",
        "detectedType": "action_v1",
        "fromTemplate": "FZmpKcQRP7hZQqbV",
        "itemId": "MWH6Za46Bjii884i",
        "itemName": "Fulgur Finis",
        "pickedOldProps": {
          "check_bonus": "0",
          "class": "Elementalist",
          "cost": "30 x T MP",
          "damage_bonus": "30",
          "description": "<p>You call fourth a massive lightning storm. Each target hit by this spell suffers <strong>【HR + 30】</strong>bolt damage.</p><p><strong>Opportunity:</strong> Each target hit by this spell suffers <span style=\"font-family: Signika, sans-serif\"><a class=\"content-link\" draggable=\"true\" data-link=\"\" data-uuid=\"JournalEntry.GqqgHkKOs0hkghdG\" data-id=\"GqqgHkKOs0hkghdG\" data-type=\"JournalEntry\" data-tooltip=\"Journal Entry\" aria-describedby=\"tooltip\" style=\"box-sizing: border-box; user-select: text; scrollbar-width: thin; scrollbar-color: rgb(93, 20, 43) rgba(0, 0, 0, 0); cursor: pointer; --content-link-background: rgba(0, 0, 0, 0.1); background-image: none !important; background-position: initial !important; background-size: initial !important; background-repeat: initial !important; background-attachment: initial !important; background-origin: initial !important; background-clip: initial !important; background-color: rgb(102, 102, 102); padding: 0px !important; border: none !important; border-radius: 2px; white-space: nowrap; word-break: break-all; color: rgb(25, 24, 19); font-weight: bold; text-decoration: underline; display: inline-flex; align-items: center; position: relative; top: 2px; font-family: Signika, sans-serif; font-size: 14px; font-style: normal; font-variant-ligatures: normal; font-variant-caps: normal; letter-spacing: normal; orphans: 2; text-align: start; text-indent: 0px; text-transform: none; widows: 2; word-spacing: 0px; -webkit-text-stroke-width: 0px;\"><strong><span style=\"text-decoration: underline;\">Paralyzed</span></strong></a></span></p>",
          "discipline": "elementalism",
          "duration": "Instantaneous",
          "id": "MWH6Za46Bjii884i",
          "img": "https://assets.forge-vtt.com/610d918102e7ac281373ffcb/Skill%20Icon/FFXIVIcons%20Battle(PvE)/13_BLM/thunder_iv.png",
          "name": "Fulgur Finis",
          "rolled_atr1": "INS",
          "rolled_atr2": "WLP",
          "target": "Up to three creatures",
          "type_damage": "bolt",
          "uuid": "Item.MWH6Za46Bjii884i"
        },
        "ts": 1770020388753
      }
    }
  },
  "folder": "S67qnepouidEkCm0",
  "img": "https://assets.forge-vtt.com/610d918102e7ac281373ffcb/Skill%20Icon/FFXIVIcons%20Battle(PvE)/13_BLM/thunder_iv.png",
  "name": "Fulgur Finis",
  "ownership": {
    "2vy7wrEUqBy0kzsU": 3,
    "6DmgigM5jq76r0Gd": 3,
    "7hKw8KGsimboeDif": 3,
    "9sFgrDy4MvAgoCmQ": 3,
    "Am7qxPOavfKeDGPd": 3,
    "JQGNzKpDPHJmcUIW": 3,
    "JvF9tXvr7aaiS06m": 3,
    "KD5d0efqHETPpIux": 3,
    "LT3Z8EnRdMVk5ncQ": 3,
    "MwTL6zXFCEpVBTpI": 3,
    "NozW36XIGEGx7oVH": 3,
    "NpGkA7uxaarkPiwS": 3,
    "ULBTZPjtza939Mvy": 3,
    "WQGFvLStTZeWGpak": 3,
    "default": 3,
    "eGu3tuw7HwJd2bpx": 3,
    "iLeUVmwqnze8ulOI": 3,
    "mE5aEkDlrQJIYyhG": 3,
    "nSGZ1sz1rdKao0kz": 3,
    "nXC7OD7u5Zw5UDDU": 3,
    "t41knaXNbgCEygM5": 3,
    "tz9BP9MKfNGf2P3n": 3,
    "wNzPmItLYCKE1I8t": 3,
    "yL2PQxh3R9ddt0te": 3,
    "yUfwoVmEPVdFG3AY": 3
  },
  "sort": 200000,
  "system": {
    "container": null,
    "props": {
      "active_effect_config_table": {},
      "animation_damage_timing_offset": "0",
      "animation_damage_timing_options": "default",
      "animation_script": "<p>// =============================================================================</p><p>// Fulgur Finis (bespoke). Parametric via a CFG object. Sibling of Ignis Finis.</p><p>// Dim the scene -&gt; multiple blue lightning bolts rush downward onto the</p><p>// target(s), bottom tip landing on each target -&gt; a large blue explosion at the</p><p>// end with a heavy screenshake AND a per-target token shake. doneHook fires at</p><p>// the explosion peak; then the dim fades out. Tier-3 \"big spell\" feel.</p><p>// =============================================================================</p><p>(async () =&gt; {</p><p></p><p>const CFG = {\"name\":\"Fulgur Finis\",\"key\":\"master_fulgurfinis\",\"dimAlpha\":0.6,\"dimMs\":420,\"undimMs\":520,\"shimmerColor\":11691775,\"shimmerAlpha\":0.3,\"shimmerMs\":700,\"boltCount\":4,\"boltSize\":1.2,\"boltSpread\":0.12,\"boltAnchorX\":0.5,\"boltAnchorY\":0.5,\"boltAnchorDrop\":0.0,\"boltAngleJitter\":70,\"boltStagger\":110,\"boltHoldMs\":150,\"boltFadeMs\":240,\"boltShakeMs\":220,\"boltShakePx\":6,\"maxBoltSfx\":3,\"strikeSpanMs\":760,\"explosionSize\":1.15,\"explosionMaxMs\":1600,\"doneAtMs\":220,\"shakeMs\":640,\"shakePx\":13,\"tokenShakeMs\":520,\"tokenShakePx\":11,\"fadeMs\":320,\"totalTimeoutMs\":14000,\"sfxBolt\":\"https://assets.forge-vtt.com/610d918102e7ac281373ffcb/Sound/ExecutionGET.wav\",\"sfxBoltVol\":0.5,\"sfxExplosion\":\"https://assets.forge-vtt.com/610d918102e7ac281373ffcb/Sound/ExecutionGET.wav\",\"sfxExplosionVol\":0.75};</p><p>const TAG      = \"[ONI][Anim][FulgurFinis][\" + CFG.name + \"]\";</p><p>const ANIM_KEY = \"oni.gen.fulgurfinis.\" + CFG.key;</p><p>const BOLT_URL = \"modules/JB2A_DnD5e/Library/Generic/Lightning/LightningStrike01_01_Regular_Blue_800x800.webm\";</p><p>const EXPLOSION_URL = \"modules/JB2A_DnD5e/Library/Generic/Explosion/Explosion_02_Blue_400x400.webm\";</p><p></p><p>const P = globalThis.__PAYLOAD ?? payload ?? {};</p><p>function tokFromUuid(u) { try { const d = fromUuidSync?.(u); return d?.object ?? null; } catch (_) { return null; } }</p><p>function resolveSource() {</p><p>  for (const c of [P?.casterTokenUuid, P?.sourceTokenUuid, P?.tokenUuid]) { const t = tokFromUuid(c); if (t) return t; }</p><p>  return canvas?.tokens?.controlled?.[0] ?? null;</p><p>}</p><p>function resolveTargets() {</p><p>  const out = [];</p><p>  const arg = Array.isArray(targets) ? targets : [];</p><p>  for (const t of arg) if (t?.center) out.push(t);</p><p>  if (!out.length &amp;&amp; Array.isArray(globalThis.__TARGETS)) for (const t of globalThis.__TARGETS) if (t?.center) out.push(t);</p><p>  return out;</p><p>}</p><p>const sourceToken  = resolveSource();</p><p>const partyTargets = resolveTargets();</p><p></p><p>const doneHook = \"oni.anim.\" + ANIM_KEY + \".\" + (foundry?.utils?.randomID?.(8) ?? String(Date.now()));</p><p>function onceHook(name, timeoutMs) {</p><p>  return new Promise((resolve) =&gt; {</p><p>    let settled = false;</p><p>    const timer = setTimeout(() =&gt; { if (settled) return; settled = true; Hooks.off(name, h); resolve({ ok: false, timeout: true }); }, timeoutMs);</p><p>    const h = (d) =&gt; { if (settled) return; settled = true; clearTimeout(timer); Hooks.off(name, h); resolve(d ?? { ok: true }); };</p><p>    Hooks.on(name, h);</p><p>  });</p><p>}</p><p>function emitLocal(name, data) { try { Hooks.callAll(name, data); } catch (_) {} }</p><p></p><p>const scriptSource = String.raw`</p><p>const caster = ctx.casterToken;</p><p>const targetTokens = ctx.targetTokens || [];</p><p>const cfg = ctx.params.cfg;</p><p>const A = ctx.params.assets;</p><p></p><p>const stage = canvas.stage;</p><p>stage.sortableChildren = true;</p><p>const ticker = canvas.app.ticker;</p><p>const W = canvas.app.renderer.screen.width;</p><p>const H = canvas.app.renderer.screen.height;</p><p>const WT = stage.worldTransform.clone();</p><p>const ZOOM = Math.abs(stage.scale?.x || 1) || 1;</p><p>const S2W = (fx, fy) =&gt; WT.applyInverse(new PIXI.Point(fx * W, fy * H));</p><p>const wLen = (px) =&gt; px / ZOOM;</p><p>const hPx  = (frac) =&gt; wLen(frac * H);</p><p>const waitMs = (ms) =&gt; new Promise(r =&gt; setTimeout(r, Number(ms) || 0));</p><p></p><p>const spawned = [];</p><p>function destroyObj(o) { try { if (o &amp;&amp; !o._destroyed) { if (o.parent) o.parent.removeChild(o); o.destroy({ children: true }); } } catch (_) {} }</p><p>function cleanupAll() { for (const o of spawned.splice(0)) destroyObj(o); }</p><p>let doneFired = false;</p><p>function fireDone(res) { if (doneFired) return; doneFired = true; try { Hooks.callAll(ctx.params.doneHook, res || { ok: true }); } catch (_) {} }</p><p></p><p>const easeOutCubic = (t) =&gt; 1 - Math.pow(1 - t, 3);</p><p>const easeInCubic  = (t) =&gt; t * t * t;</p><p>function tween(fn, dur, ease) {</p><p>  return new Promise((resolve) =&gt; {</p><p>    const t0 = performance.now();</p><p>    const step = () =&gt; { const raw = dur&gt;0?Math.min(1,(performance.now()-t0)/dur):1; try{fn(ease?ease(raw):raw,raw);}catch(_){} if(raw&gt;=1){ticker.remove(step);resolve();} };</p><p>    ticker.add(step);</p><p>  });</p><p>}</p><p>function fullRect(color, alpha, z, blend) {</p><p>  const a = S2W(-0.1,-0.1), b = S2W(1.1,1.1);</p><p>  const g = new PIXI.Graphics();</p><p>  g.beginFill(color, 1).drawRect(a.x, a.y, b.x-a.x, b.y-a.y).endFill();</p><p>  g.alpha = alpha; g.zIndex = z; if (blend) g.blendMode = PIXI.BLEND_MODES.ADD;</p><p>  return g;</p><p>}</p><p>function screenShake(ms, px) {</p><p>  try {</p><p>    const view = canvas.app.view;</p><p>    if (!document.getElementById(\"oni-shake-kf\")) {</p><p>      const st = document.createElement(\"style\"); st.id = \"oni-shake-kf\";</p><p>      st.textContent = \"@keyframes oniShake{0%,100%{transform:translate(0,0)}15%{transform:translate(-\" + px + \"px,\" + px + \"px)}30%{transform:translate(\" + px + \"px,-\" + px + \"px)}45%{transform:translate(-\" + px + \"px,-\" + px + \"px)}60%{transform:translate(\" + px + \"px,\" + px + \"px)}75%{transform:translate(-\" + px + \"px,0)}} .oni-shaking{animation:oniShake 0.42s linear}\";</p><p>      document.head.appendChild(st);</p><p>    }</p><p>    view.classList.remove(\"oni-shaking\"); void view.offsetWidth; view.classList.add(\"oni-shaking\");</p><p>    setTimeout(() =&gt; { try { view.classList.remove(\"oni-shaking\"); } catch(_){} }, ms);</p><p>  } catch (_) {}</p><p>}</p><p>async function loadVideo(url) {</p><p>  const vid = document.createElement(\"video\");</p><p>  try { vid.crossOrigin = \"anonymous\"; } catch (_) {}</p><p>  vid.src = url; vid.muted = true; vid.loop = false; vid.playsInline = true; vid.preload = \"auto\";</p><p>  await new Promise((res) =&gt; { let done=false; const go=()=&gt;{if(done)return;done=true;res();}; vid.addEventListener(\"loadeddata\",go,{once:true}); vid.addEventListener(\"error\",go,{once:true}); setTimeout(go,1500); });</p><p>  return vid;</p><p>}</p><p>function playSfx(url, vol) {</p><p>  if (!url) return;</p><p>  try { const a = new Audio(url); a.volume = (vol == null ? 0.6 : vol); a.play().catch(() =&gt; {}); } catch (_) {}</p><p>}</p><p>function meshOf(tok) { return tok ? tok.mesh : null; }</p><p>function makeSprite(tok) {</p><p>  const m = meshOf(tok);</p><p>  const sp = new PIXI.Sprite(m?.texture ?? PIXI.Texture.WHITE);</p><p>  sp.anchor.set(0.5, 0.5);</p><p>  sp.width = Math.abs(m?.width ?? (tok?.w ?? 100));</p><p>  sp.height = Math.abs(m?.height ?? (tok?.h ?? 100));</p><p>  // Match the rendered token's mirror: mesh.scale sign folds in Mirror-Horizontal + facing.</p><p>  const sgx = Math.sign(m?.scale?.x || 1) || 1;</p><p>  const sgy = Math.sign(m?.scale?.y || 1) || 1;</p><p>  sp.scale.x = Math.abs(sp.scale.x) * sgx;</p><p>  sp.scale.y = Math.abs(sp.scale.y) * sgy;</p><p>  sp.angle = m?.angle ?? 0;</p><p>  return sp;</p><p>}</p><p>function makeHider(tok) {</p><p>  const m = meshOf(tok); let on = false, tR = true, mR = true;</p><p>  const guard = () =&gt; { if (!tok) return; tok.renderable = false; if (m) m.renderable = false; };</p><p>  return {</p><p>    start() { if (!tok || on) return; on = true; tR = tok.renderable ?? true; mR = m?.renderable ?? true; ticker.add(guard); },</p><p>    restore() { if (!tok || !on) return; ticker.remove(guard); tok.renderable = tR; if (m) m.renderable = mR; try { tok.renderFlags.set({ refreshVisibility: true, refreshMesh: true, refreshState: true }); } catch (_) {} on = false; },</p><p>  };</p><p>}</p><p>async function tokenShake(tok, ms, px) {</p><p>  if (!tok) return;</p><p>  const hide = makeHider(tok);</p><p>  const c = { x: tok.center.x, y: tok.center.y };</p><p>  const spr = makeSprite(tok);</p><p>  spr.x = c.x; spr.y = c.y; spr.zIndex = 8900;</p><p>  try {</p><p>    hide.start();</p><p>    stage.addChild(spr); spawned.push(spr);</p><p>    await tween((p) =&gt; { const d = 1 - p; spr.x = c.x + (Math.random()*2-1)*px*d; spr.y = c.y + (Math.random()*2-1)*px*d; }, ms, null);</p><p>  } catch (_) {} finally {</p><p>    destroyObj(spr); hide.restore();</p><p>  }</p><p>}</p><p></p><p>try {</p><p>  const points = (targetTokens.length ? targetTokens : [caster])</p><p>    .filter(Boolean)</p><p>    .map(t =&gt; ({ x: t.center.x, y: t.center.y, tok: t }));</p><p>  if (!points.length) points.push({ x: S2W(0.6,0.5).x, y: S2W(0.6,0.5).y, tok: null });</p><p></p><p>  // 1) dim the scene</p><p>  const dim = fullRect(0x000000, 0, 7000, false);</p><p>  stage.addChild(dim); spawned.push(dim);</p><p>  await tween((p) =&gt; { dim.alpha = cfg.dimAlpha * p; }, cfg.dimMs, easeOutCubic);</p><p></p><p>  // 1b) element-colored screen shimmer at initialization (mirrors Ignis Finis)</p><p>  const shim = fullRect(cfg.shimmerColor, 0, 7100, true);</p><p>  stage.addChild(shim); spawned.push(shim);</p><p>  tween((p) =&gt; { shim.alpha = Math.sin(p*Math.PI) * cfg.shimmerAlpha; }, cfg.shimmerMs, null).then(() =&gt; destroyObj(shim));</p><p></p><p>  // 2) multiple lightning bolts rush down onto each target</p><p>  let boltSfxCount = 0;</p><p>  async function boltStrike(pt) {</p><p>    for (let i = 0; i &lt; cfg.boltCount; i++) {</p><p>      const isLast = (i === cfg.boltCount - 1);</p><p>      const jx = isLast ? 0 : (Math.random()*2 - 1) * hPx(cfg.boltSpread);</p><p>      (async () =&gt; {</p><p>        const vid = await loadVideo(A.bolt);</p><p>        const tex = PIXI.Texture.from(vid);</p><p>        const nw = tex.width || tex.orig?.width || 800;</p><p>        const sp = new PIXI.Sprite(tex);</p><p>        sp.anchor.set(cfg.boltAnchorX, cfg.boltAnchorY);</p><p>        sp.x = pt.x + jx; sp.y = pt.y + hPx(cfg.boltAnchorDrop); sp.zIndex = 8100;</p><p>        sp.scale.set(hPx(cfg.boltSize) / nw);</p><p>        // vary the strike angle but stay within the top hemisphere so it always falls from the sky</p><p>        sp.angle = (Math.random()*2 - 1) * cfg.boltAngleJitter;</p><p>        sp.blendMode = PIXI.BLEND_MODES.ADD;</p><p>        stage.addChild(sp); spawned.push(sp);</p><p>        try { vid.currentTime = 0; await vid.play(); } catch (_) {}</p><p>        if (boltSfxCount &lt; cfg.maxBoltSfx) { boltSfxCount++; playSfx(cfg.sfxBolt, cfg.sfxBoltVol); }</p><p>        if (cfg.boltShakePx) screenShake(cfg.boltShakeMs, cfg.boltShakePx);</p><p>        await waitMs(cfg.boltHoldMs);</p><p>        await tween((p) =&gt; { sp.alpha = 1 - p; }, cfg.boltFadeMs, easeOutCubic);</p><p>        destroyObj(sp);</p><p>        try { vid.pause(); vid.removeAttribute(\"src\"); vid.load(); } catch (_) {}</p><p>      })();</p><p>      await waitMs(cfg.boltStagger);</p><p>    }</p><p>  }</p><p>  points.forEach(pt =&gt; { boltStrike(pt); });</p><p>  await waitMs(cfg.strikeSpanMs);</p><p></p><p>  // 3) large explosion at each target + heavy shakes</p><p>  const exSprites = [];</p><p>  const exVids = [];</p><p>  for (const pt of points) {</p><p>    const vid = await loadVideo(A.explosion);</p><p>    const tex = PIXI.Texture.from(vid);</p><p>    const nw = tex.width || tex.orig?.width || 400;</p><p>    const sp = new PIXI.Sprite(tex);</p><p>    sp.anchor.set(0.5, 0.5); sp.x = pt.x; sp.y = pt.y; sp.zIndex = 8200;</p><p>    sp.scale.set(hPx(cfg.explosionSize) / nw);</p><p>    sp.blendMode = PIXI.BLEND_MODES.ADD;</p><p>    stage.addChild(sp); spawned.push(sp);</p><p>    try { vid.currentTime = 0; await vid.play(); } catch (_) {}</p><p>    exSprites.push(sp); exVids.push(vid);</p><p>  }</p><p>  playSfx(cfg.sfxExplosion, cfg.sfxExplosionVol);</p><p>  screenShake(cfg.shakeMs, cfg.shakePx);</p><p>  points.forEach(pt =&gt; { if (pt.tok) tokenShake(pt.tok, cfg.tokenShakeMs, cfg.tokenShakePx); });</p><p></p><p>  await waitMs(cfg.doneAtMs);</p><p>  fireDone({ ok: true });</p><p></p><p>  await new Promise((res) =&gt; { let done=false; const go=()=&gt;{if(done)return;done=true;res();}; if (exVids[0]) exVids[0].addEventListener(\"ended\",go,{once:true}); setTimeout(go, cfg.explosionMaxMs); });</p><p>  await tween((p) =&gt; { for (const sp of exSprites) sp.alpha = 1 - p; }, cfg.fadeMs, easeOutCubic);</p><p>  for (const sp of exSprites) destroyObj(sp);</p><p>  for (const vid of exVids) { try { vid.pause(); vid.removeAttribute(\"src\"); vid.load(); } catch (_) {} }</p><p></p><p>  // 4) undim</p><p>  await tween((p) =&gt; { dim.alpha = cfg.dimAlpha * (1 - p); }, cfg.undimMs, easeOutCubic);</p><p>  cleanupAll();</p><p>} catch (err) {</p><p>  console.error(\"[ONI][Pseudo][FulgurFinis]\", err);</p><p>  fireDone({ ok: false, error: String(err &amp;&amp; err.message || err) });</p><p>} finally {</p><p>  cleanupAll();</p><p>}</p><p>`;</p><p></p><p>try {</p><p>  emitLocal(\"oni:animationStart\", { type: ANIM_KEY, sceneId: canvas.scene?.id ?? null });</p><p>  const waitDone = onceHook(doneHook, CFG.totalTimeoutMs || 14000);</p><p>  await game.ONI.pseudo.play({</p><p>    scriptId: ANIM_KEY,</p><p>    scriptSource,</p><p>    casterTokenUuid: sourceToken?.document?.uuid ?? null,</p><p>    targetTokenUuids: partyTargets.map(t =&gt; t.document.uuid),</p><p>    params: { doneHook, cfg: CFG, assets: { bolt: BOLT_URL, explosion: EXPLOSION_URL } },</p><p>    meta: { source: CFG.name },</p><p>  });</p><p>  const result = await waitDone;</p><p>  if (!result?.ok) console.warn(TAG, \"pseudo non-OK / timeout\", result);</p><p>} catch (err) {</p><p>  console.error(TAG, \"orchestration crashed\", err);</p><p>} finally {</p><p>  emitLocal(\"oni:animationEnd\", { type: ANIM_KEY, timestamp: Date.now() });</p><p>}</p><p></p><p>})();</p><p></p>",
      "check_bonus": "0",
      "class": "Elementalist",
      "cost": "30 x T MP",
      "damage_bonus": "35",
      "defense_target_type": "mdef",
      "description": "<p>You call fourth a massive lightning storm. Each target hit by this spell suffers <strong>【HR + 35】</strong>bolt damage.</p><p><strong>Opportunity:</strong> Each target hit by this spell suffers <span style=\"text-decoration: underline;\"><span style=\"font-family: Signika, sans-serif\"><a class=\"content-link\" draggable=\"true\" data-link=\"\" data-uuid=\"JournalEntry.GqqgHkKOs0hkghdG\" data-id=\"GqqgHkKOs0hkghdG\" data-type=\"JournalEntry\" data-tooltip=\"Journal Entry\" aria-describedby=\"tooltip\" style=\"box-sizing: border-box; user-select: text; scrollbar-width: thin; scrollbar-color: rgb(93, 20, 43) rgba(0, 0, 0, 0); cursor: pointer; --content-link-background: rgba(0, 0, 0, 0.1); background-image: none !important; background-position: initial !important; background-size: initial !important; background-repeat: initial !important; background-attachment: initial !important; background-origin: initial !important; background-clip: initial !important; background-color: rgb(102, 102, 102); padding: 0px !important; border: none !important; border-radius: 2px; white-space: nowrap; word-break: break-all; color: rgb(25, 24, 19); font-weight: bold; text-decoration: underline; display: inline-flex; align-items: center; position: relative; top: 2px; font-family: Signika, sans-serif; font-size: 14px; font-style: normal; font-variant-ligatures: normal; font-variant-caps: normal; letter-spacing: normal; orphans: 2; text-align: start; text-indent: 0px; text-transform: none; widows: 2; word-spacing: 0px; -webkit-text-stroke-width: 0px;\"><strong><span style=\"text-decoration: underline;\">Paralyzed</span></strong></a></span></span></p>",
      "details_roller": "Show",
      "duration": "Instantaneous",
      "effect_table": {},
      "heroic_requirement": "",
      "id": "MWH6Za46Bjii884i",
      "ignore_hr": false,
      "img": "https://assets.forge-vtt.com/610d918102e7ac281373ffcb/Skill%20Icon/FFXIVIcons%20Battle(PvE)/13_BLM/thunder_iv.png",
      "isCheck": true,
      "isFacet": false,
      "isHeroic": false,
      "isOffensiveSpell": true,
      "isReaction": false,
      "isZeroPower": false,
      "level": "1",
      "max_level": "1",
      "name": "Fulgur Finis",
      "on_activate_effect_ref": "",
      "optional_params": {},
      "reaction_config_table": {},
      "rolled_atr1": "INS",
      "rolled_atr2": "WLP",
      "skill_animation_default": "",
      "skill_animation_mode": "default",
      "skill_information": "",
      "skill_range": "",
      "skill_target": "Up to three creatures",
      "skill_type": "Spell",
      "type_damage": "bolt",
      "use_optional_params": false,
      "uuid": "Item.MWH6Za46Bjii884i"
    },
    "template": "j0F5Msw5RZ8aIB3j",
    "templateSystemUniqueVersion": 2788278387,
    "unique": false,
    "uniqueId": "MWH6Za46Bjii884i"
  },
  "type": "equippableItem"
}
