{
  "_id": "CmyctfYXyvsBmRuf",
  "_stats": {
    "compendiumSource": "Item.StNZ1ldqagBVx41h",
    "coreVersion": "12.343",
    "createdTime": 1782072258385,
    "duplicateSource": null,
    "systemId": "custom-system-builder",
    "systemVersion": "4.8.5"
  },
  "flags": {
    "custom-system-builder": {
      "version": "4.6.4"
    }
  },
  "folder": "Lmz3zk1ikDBWqyf1",
  "img": "https://assets.forge-vtt.com/610d918102e7ac281373ffcb/Skill%20Icon/Tree%20of%20Savior/Swordsman/icon_warri_cartarstroke.png",
  "name": "Descabello",
  "ownership": {
    "JQGNzKpDPHJmcUIW": 3,
    "default": 0
  },
  "sort": 0,
  "system": {
    "props": {
      "active_effect_config_table": {},
      "animation_damage_timing_offset": "0",
      "animation_damage_timing_options": "default",
      "animation_script": "<p>// ============================================================================</p><p>// Descabello — Blackout Spotlight Blink-Rush Behind Target</p><p>// Foundry V12 / Oni Action Pipeline</p><p>//</p><p>// Visuals: ONI Pseudo Animation</p><p>// SFX: Sequencer sound only</p><p>//</p><p>// Suggested Damage Timing:</p><p>// animation_damage_timing_options = \"offset\"</p><p>// animation_damage_timing_offset = 2785</p><p>// ============================================================================</p><p>return (async () =&gt; {</p><p>const TAG = \"[ONI][Anim][Descabello]\";</p><p>// ─────────────────────────────────────────────────────────────</p><p>// TUNING KNOBS</p><p>// ─────────────────────────────────────────────────────────────</p><p>const PRE_RUSH_DELAY_MS = 2500;</p><p>const FLASH_IN_MS = 55;</p><p>const FLASH_OUT_MS = 110;</p><p>const RUSH_MS = 120;</p><p>const AFTERIMAGE_INTERVAL_MS = 18;</p><p>const AFTERIMAGE_LIFE_MS = 240;</p><p>const HOLD_AFTER_IMPACT_MS = 2000;</p><p>const BLACKOUT_FADE_IN_MS = 320;</p><p>const BLACKOUT_FADE_OUT_MS = 550;</p><p>const RETURN_JUMP_MS = 520;</p><p>const RETURN_JUMP_HEIGHT = 90;</p><p>const BEHIND_TARGET_DISTANCE = 125;</p><p>const TARGET_KNOCKBACK_DISTANCE = 42;</p><p>const TARGET_FEEDBACK_MS = 420;</p><p>const IMPACT_VFX = \"modules/JB2A_DnD5e/Library/Generic/Impact/Impact_04_Regular_Blue_400x400.webm\";</p><p>const IMPACT_VFX_SCALE = 2;</p><p>const IMPACT_VFX_MS = 900;</p><p>const BLACKOUT_START_SFX = \"https://assets.forge-vtt.com/610d918102e7ac281373ffcb/Sound/Soundboard/Battle3.ogg\";</p><p>const BLACKOUT_START_SFX_VOLUME = 0.75;</p><p>const IMPACT_SFX = \"https://assets.forge-vtt.com/610d918102e7ac281373ffcb/Sound/Hit_SlashingB.wav\";</p><p>const IMPACT_SFX_VOLUME = 0.9;</p><p>const SPOTLIGHT_CASTER_RADIUS_PX = 115;</p><p>const SPOTLIGHT_TARGET_RADIUS_PX = 130;</p><p>const BLACKOUT_OPACITY = 0.88;</p><p>// z-index stack inside PIXI canvas.</p><p>// Blackout is intentionally below all skill VFX.</p><p>const Z_BLACKOUT = 7000;</p><p>const Z_CASTER = 8000;</p><p>const Z_AFTERIMAGE = 7999;</p><p>const Z_TARGET_FEEDBACK = 8100;</p><p>const Z_IMPACT = 8200;</p><p>const Z_DOM_FLASH = 2147483000;</p><p>const IMPACT_AT_MS = PRE_RUSH_DELAY_MS + FLASH_IN_MS + FLASH_OUT_MS + RUSH_MS;</p><p>const TOTAL_MS =</p><p>IMPACT_AT_MS +</p><p>HOLD_AFTER_IMPACT_MS +</p><p>BLACKOUT_FADE_OUT_MS +</p><p>RETURN_JUMP_MS +</p><p>350;</p><p>// ─────────────────────────────────────────────────────────────</p><p>// Basic helpers</p><p>// ─────────────────────────────────────────────────────────────</p><p>const wait = (ms) =&gt; new Promise(resolve =&gt; setTimeout(resolve, ms));</p><p>globalThis.ONI = globalThis.ONI ?? {};</p><p>if (!ONI.emit) {</p><p>ONI.emit = function (eventName, payload = {}, options = {}) {</p><p>const { local = true, world = false } = options;</p><p>if (local) Hooks.callAll(eventName, payload);</p><p>if (world) game.socket.emit(\"world\", { action: eventName, payload });</p><p>};</p><p>}</p><p>function toTokenPlaceable(x) {</p><p>if (!x) return null;</p><p>if (x?.document?.documentName === \"Token\") return x;</p><p>if (x?.documentName === \"Token\") return x.object ?? null;</p><p>if (x?.object?.document?.documentName === \"Token\") return x.object;</p><p>return null;</p><p>}</p><p>async function tokenFromUuidish(uuidish) {</p><p>if (!uuidish || typeof uuidish !== \"string\") return null;</p><p>try {</p><p>const doc = await fromUuid(uuidish);</p><p>if (!doc) return null;</p><p>if (doc.documentName === \"Token\") return doc.object ?? null;</p><p>if (doc.documentName === \"Actor\") {</p><p>return doc.getActiveTokens?.(true, true)?.[0] ?? null;</p><p>}</p><p>if (doc.actor?.getActiveTokens) {</p><p>return doc.actor.getActiveTokens(true, true)?.[0] ?? null;</p><p>}</p><p>return toTokenPlaceable(doc);</p><p>} catch (err) {</p><p>console.warn(TAG, \"Could not resolve token from UUID:\", uuidish, err);</p><p>return null;</p><p>}</p><p>}</p><p>async function resolveCasterToken(p) {</p><p>const refs = [</p><p>p?.sourceTokenUuid,</p><p>p?.casterTokenUuid,</p><p>p?.tokenUuid,</p><p>p?.tokenUUID,</p><p>p?.meta?.sourceTokenUuid,</p><p>p?.meta?.casterTokenUuid,</p><p>p?.attackerUuid,</p><p>p?.attackerActorUuid,</p><p>p?.meta?.attackerUuid,</p><p>p?.meta?.attackerActorUuid</p><p>].filter(Boolean);</p><p>for (const ref of refs) {</p><p>const tok = await tokenFromUuidish(ref);</p><p>if (tok) return tok;</p><p>}</p><p>return null;</p><p>}</p><p>async function resolveTargetToken(p, rawTargets) {</p><p>const directTargets = Array.isArray(rawTargets) ? rawTargets : [];</p><p>for (const t of directTargets) {</p><p>const direct = toTokenPlaceable(t);</p><p>if (direct) return direct;</p><p>const uuid =</p><p>typeof t === \"string\"</p><p>? t</p><p>: t?.document?.uuid ?? t?.uuid ?? null;</p><p>const tok = await tokenFromUuidish(uuid);</p><p>if (tok) return tok;</p><p>}</p><p>const refs = [</p><p>...(Array.isArray(p?.targetTokenUuids) ? p.targetTokenUuids : []),</p><p>...(Array.isArray(p?.targets) ? p.targets : []),</p><p>...(Array.isArray(p?.originalTargetUUIDs) ? p.originalTargetUUIDs : []),</p><p>...(Array.isArray(p?.meta?.targetTokenUuids) ? p.meta.targetTokenUuids : []),</p><p>...(Array.isArray(p?.meta?.originalTargetUUIDs) ? p.meta.originalTargetUUIDs : [])</p><p>].filter(Boolean);</p><p>for (const ref of refs) {</p><p>const tok = await tokenFromUuidish(ref);</p><p>if (tok) return tok;</p><p>}</p><p>return null;</p><p>}</p><p>const p = payload ?? globalThis.__PAYLOAD ?? {};</p><p>const sourceToken = await resolveCasterToken(p);</p><p>const targetToken = await resolveTargetToken(p, targets ?? globalThis.__TARGETS ?? []);</p><p>if (!sourceToken) {</p><p>ui.notifications?.warn?.(\"Descabello animation: could not resolve caster token from payload.\");</p><p>return false;</p><p>}</p><p>if (!targetToken) {</p><p>ui.notifications?.warn?.(\"Descabello animation: could not resolve target token from payload.\");</p><p>return false;</p><p>}</p><p>if (!game.ONI?.pseudo?.play) {</p><p>ui.notifications?.error?.(\"Descabello animation: ONI Pseudo Animation API not found.\");</p><p>return false;</p><p>}</p><p>ONI.emit(\"oni:animationStart\", {</p><p>type: \"descabello\",</p><p>sourceTokenId: sourceToken.id,</p><p>targetTokenIds: [targetToken.id],</p><p>sceneId: canvas.scene?.id ?? null,</p><p>timestamp: Date.now()</p><p>}, { local: true, world: false });</p><p>try {</p><p>if (game.modules.get(\"sequencer\")?.active) {</p><p>new Sequence()</p><p>.sound()</p><p>.file(BLACKOUT_START_SFX)</p><p>.delay(0)</p><p>.volume(BLACKOUT_START_SFX_VOLUME)</p><p>.sound()</p><p>.file(IMPACT_SFX)</p><p>.delay(IMPACT_AT_MS)</p><p>.volume(IMPACT_SFX_VOLUME)</p><p>.play();</p><p>} else {</p><p>console.warn(TAG, \"Sequencer is not active, SFX skipped.\");</p><p>}</p><p>const scriptSource = `</p><p>const { casterToken, targetTokens, params } = ctx;</p><p>const targetToken = targetTokens &amp;&amp; targetTokens.length ? targetTokens[0] : null;</p><p>if (!casterToken) throw new Error(\"Descabello pseudo: missing casterToken.\");</p><p>if (!targetToken) throw new Error(\"Descabello pseudo: missing targetToken.\");</p><p>const wait = (ms) =&gt; new Promise(resolve =&gt; setTimeout(resolve, ms));</p><p>function clamp01(v) {</p><p>return Math.max(0, Math.min(1, Number(v) || 0));</p><p>}</p><p>function easeInOutCubic(t) {</p><p>t = clamp01(t);</p><p>return t &lt; 0.5</p><p>? 4 * t * t * t</p><p>: 1 - Math.pow(-2 * t + 2, 3) / 2;</p><p>}</p><p>function easeOutCubic(t) {</p><p>t = clamp01(t);</p><p>return 1 - Math.pow(1 - t, 3);</p><p>}</p><p>function tokenCenter(token) {</p><p>return {</p><p>x: token.center?.x ?? token.x ?? 0,</p><p>y: token.center?.y ?? token.y ?? 0</p><p>};</p><p>}</p><p>function getDisp(token) {</p><p>const d =</p><p>token?.document?.disposition ??</p><p>token?.actor?.document?.disposition ??</p><p>token?.actor?.prototypeToken?.disposition ??</p><p>1;</p><p>return d === -1 ? -1 : 1;</p><p>}</p><p>function rushDirectionFromCaster(token) {</p><p>return getDisp(token) === -1 ? 1 : -1;</p><p>}</p><p>function getBaseDisplayObject(token) {</p><p>return token.mesh ?? token.icon ?? token.children?.[0] ?? null;</p><p>}</p><p>function getTokenFacingSigns(token, base = null) {</p><p>const tex = token?.document?.texture ?? {};</p><p>const baseScaleX = Number(base?.scale?.x ?? 1);</p><p>const baseScaleY = Number(base?.scale?.y ?? 1);</p><p>const docScaleX = Number(tex?.scaleX ?? 1);</p><p>const docScaleY = Number(tex?.scaleY ?? 1);</p><p>const mirrorX =</p><p>docScaleX &lt; 0 ||</p><p>baseScaleX &lt; 0 ||</p><p>tex?.mirrorX === true ||</p><p>tex?.mirrorHorizontal === true ||</p><p>tex?.mirroredX === true;</p><p>const mirrorY =</p><p>docScaleY &lt; 0 ||</p><p>baseScaleY &lt; 0 ||</p><p>tex?.mirrorY === true ||</p><p>tex?.mirrorVertical === true ||</p><p>tex?.mirroredY === true;</p><p>return {</p><p>x: mirrorX ? -1 : 1,</p><p>y: mirrorY ? -1 : 1</p><p>};</p><p>}</p><p>function applyFacingSigns(sprite, signs) {</p><p>if (!sprite || !signs) return;</p><p>sprite.scale.x = Math.abs(sprite.scale.x || 1) * (signs.x &lt; 0 ? -1 : 1);</p><p>sprite.scale.y = Math.abs(sprite.scale.y || 1) * (signs.y &lt; 0 ? -1 : 1);</p><p>sprite.__oniFacingSigns = {</p><p>x: signs.x &lt; 0 ? -1 : 1,</p><p>y: signs.y &lt; 0 ? -1 : 1</p><p>};</p><p>}</p><p>function copySpriteFacingSigns(targetSprite, sourceSprite) {</p><p>if (!targetSprite || !sourceSprite) return;</p><p>const signs = sourceSprite.__oniFacingSigns ?? {</p><p>x: Math.sign(sourceSprite.scale?.x || 1) || 1,</p><p>y: Math.sign(sourceSprite.scale?.y || 1) || 1</p><p>};</p><p>applyFacingSigns(targetSprite, signs);</p><p>}</p><p>function hideTokenVisual(token) {</p><p>const base = getBaseDisplayObject(token);</p><p>const state = {</p><p>token,</p><p>tokenVisible: token.visible,</p><p>base,</p><p>baseVisible: base?.visible</p><p>};</p><p>if (base) base.visible = false;</p><p>token.visible = false;</p><p>return state;</p><p>}</p><p>function restoreTokenVisual(state) {</p><p>if (!state?.token) return;</p><p>if (state.base) state.base.visible = state.baseVisible;</p><p>state.token.visible = state.tokenVisible;</p><p>}</p><p>async function makeTokenSprite(token, zIndex) {</p><p>const base = getBaseDisplayObject(token);</p><p>const texture = base?.texture ?? await loadTexture(token.document.texture.src);</p><p>const signs = getTokenFacingSigns(token, base);</p><p>const sprite = new PIXI.Sprite(texture);</p><p>sprite.anchor.set(0.5);</p><p>const c = tokenCenter(token);</p><p>sprite.x = c.x;</p><p>sprite.y = c.y;</p><p>if (base) {</p><p>sprite.width = Math.abs(base.width || token.w || 100);</p><p>sprite.height = Math.abs(base.height || token.h || 100);</p><p>sprite.rotation = base.rotation ?? 0;</p><p>} else {</p><p>sprite.width = token.w ?? 100;</p><p>sprite.height = token.h ?? 100;</p><p>}</p><p>applyFacingSigns(sprite, signs);</p><p>sprite.alpha = 1;</p><p>sprite.zIndex = zIndex;</p><p>canvas.stage.sortableChildren = true;</p><p>canvas.stage.addChild(sprite);</p><p>canvas.stage.sortChildren();</p><p>return sprite;</p><p>}</p><p>function removeSprite(sprite) {</p><p>if (!sprite || sprite.destroyed) return;</p><p>try {</p><p>if (sprite.parent) sprite.parent.removeChild(sprite);</p><p>sprite.destroy();</p><p>} catch (_) {}</p><p>}</p><p>function fadeAlpha(obj, from, to, durMs) {</p><p>return new Promise(resolve =&gt; {</p><p>const start = performance.now();</p><p>obj.alpha = from;</p><p>const tick = () =&gt; {</p><p>const t = clamp01((performance.now() - start) / Math.max(1, durMs));</p><p>const e = easeInOutCubic(t);</p><p>obj.alpha = from + (to - from) * e;</p><p>if (t &gt;= 1) {</p><p>canvas.app.ticker.remove(tick);</p><p>resolve();</p><p>}</p><p>};</p><p>canvas.app.ticker.add(tick);</p><p>});</p><p>}</p><p>function animateXY(obj, fromX, fromY, toX, toY, durMs, onFrame = null) {</p><p>return new Promise(resolve =&gt; {</p><p>const start = performance.now();</p><p>const tick = () =&gt; {</p><p>const t = clamp01((performance.now() - start) / Math.max(1, durMs));</p><p>const e = easeOutCubic(t);</p><p>obj.x = fromX + (toX - fromX) * e;</p><p>obj.y = fromY + (toY - fromY) * e;</p><p>if (onFrame) onFrame(t, e, obj);</p><p>if (t &gt;= 1) {</p><p>canvas.app.ticker.remove(tick);</p><p>resolve();</p><p>}</p><p>};</p><p>canvas.app.ticker.add(tick);</p><p>});</p><p>}</p><p>function animateJump(obj, fromX, fromY, toX, toY, durMs, height) {</p><p>return new Promise(resolve =&gt; {</p><p>const start = performance.now();</p><p>const tick = () =&gt; {</p><p>const t = clamp01((performance.now() - start) / Math.max(1, durMs));</p><p>const e = easeInOutCubic(t);</p><p>obj.x = fromX + (toX - fromX) * e;</p><p>const baseY = fromY + (toY - fromY) * e;</p><p>const arc = Math.sin(Math.PI * t) * Number(height || 0);</p><p>obj.y = baseY - arc;</p><p>if (t &gt;= 1) {</p><p>canvas.app.ticker.remove(tick);</p><p>resolve();</p><p>}</p><p>};</p><p>canvas.app.ticker.add(tick);</p><p>});</p><p>}</p><p>function getWorldViewBounds(pad = 1000) {</p><p>const screen = canvas.app.renderer.screen;</p><p>const inv = canvas.stage.worldTransform.clone().invert();</p><p>const p1 = inv.apply(new PIXI.Point(0, 0));</p><p>const p2 = inv.apply(new PIXI.Point(screen.width, screen.height));</p><p>const x = Math.min(p1.x, p2.x) - pad;</p><p>const y = Math.min(p1.y, p2.y) - pad;</p><p>const w = Math.abs(p2.x - p1.x) + pad * 2;</p><p>const h = Math.abs(p2.y - p1.y) + pad * 2;</p><p>return { x, y, w, h };</p><p>}</p><p>function screenPxToWorld(px) {</p><p>const scale = Math.abs(canvas.stage.scale?.x || 1);</p><p>return Number(px || 0) / Math.max(0.0001, scale);</p><p>}</p><p>function createSpotlightOverlay() {</p><p>const container = new PIXI.Container();</p><p>container.zIndex = Number(params.zBlackout ?? 7000);</p><p>container.alpha = 0;</p><p>container.sortableChildren = true;</p><p>const gfx = new PIXI.Graphics();</p><p>container.addChild(gfx);</p><p>canvas.stage.sortableChildren = true;</p><p>canvas.stage.addChild(container);</p><p>canvas.stage.sortChildren();</p><p>fadeAlpha(container, 0, 1, Number(params.blackoutFadeInMs ?? 320));</p><p>return { container, gfx };</p><p>}</p><p>function updateSpotlight(overlay, casterObj, targetToken) {</p><p>if (!overlay?.gfx) return;</p><p>const gfx = overlay.gfx;</p><p>const bounds = getWorldViewBounds(1800);</p><p>const casterR = screenPxToWorld(params.spotlightCasterRadiusPx ?? 115);</p><p>const targetR = screenPxToWorld(params.spotlightTargetRadiusPx ?? 130);</p><p>const targetCenter = tokenCenter(targetToken);</p><p>gfx.clear();</p><p>// Blackout layer drawn in PIXI, below all skill VFX.</p><p>// beginHole/endHole creates transparent spotlight windows.</p><p>gfx.beginFill(0x000000, Number(params.blackoutOpacity ?? 0.88));</p><p>gfx.drawRect(bounds.x, bounds.y, bounds.w, bounds.h);</p><p>gfx.beginHole();</p><p>gfx.drawCircle(casterObj.x, casterObj.y, casterR);</p><p>gfx.drawCircle(targetCenter.x, targetCenter.y, targetR);</p><p>gfx.endHole();</p><p>gfx.endFill();</p><p>}</p><p>async function fadeOutSpotlightOverlay(overlay) {</p><p>if (!overlay?.container) return;</p><p>await fadeAlpha(</p><p>overlay.container,</p><p>overlay.container.alpha ?? 1,</p><p>0,</p><p>Number(params.blackoutFadeOutMs ?? 550)</p><p>);</p><p>}</p><p>function removeSpotlightOverlay(overlay) {</p><p>if (!overlay?.container) return;</p><p>try {</p><p>if (overlay.container.parent) overlay.container.parent.removeChild(overlay.container);</p><p>overlay.container.destroy({ children: true });</p><p>} catch (_) {}</p><p>}</p><p>async function whiteFlash() {</p><p>const div = document.createElement(\"div\");</p><p>Object.assign(div.style, {</p><p>position: \"fixed\",</p><p>inset: \"0\",</p><p>width: \"100vw\",</p><p>height: \"100vh\",</p><p>background: \"white\",</p><p>opacity: \"0\",</p><p>zIndex: String(params.zDomFlash ?? 2147483000),</p><p>pointerEvents: \"none\",</p><p>transition: \"opacity \" + Number(params.flashInMs ?? 55) + \"ms linear\"</p><p>});</p><p>document.body.appendChild(div);</p><p>requestAnimationFrame(() =&gt; {</p><p>div.style.opacity = \"1\";</p><p>});</p><p>await wait(Number(params.flashInMs ?? 55));</p><p>div.style.transition = \"opacity \" + Number(params.flashOutMs ?? 110) + \"ms ease-out\";</p><p>div.style.opacity = \"0\";</p><p>await wait(Number(params.flashOutMs ?? 110) + 20);</p><p>try { div.remove(); } catch (_) {}</p><p>}</p><p>function spawnAfterimage(sourceSprite, spawnedList) {</p><p>const ghost = new PIXI.Sprite(sourceSprite.texture);</p><p>ghost.anchor.set(sourceSprite.anchor.x, sourceSprite.anchor.y);</p><p>ghost.x = sourceSprite.x;</p><p>ghost.y = sourceSprite.y;</p><p>ghost.width = Math.abs(sourceSprite.width);</p><p>ghost.height = Math.abs(sourceSprite.height);</p><p>ghost.rotation = sourceSprite.rotation;</p><p>copySpriteFacingSigns(ghost, sourceSprite);</p><p>ghost.alpha = 0.42;</p><p>ghost.tint = 0x9edfff;</p><p>ghost.zIndex = Number(params.zAfterimage ?? 7999);</p><p>canvas.stage.addChild(ghost);</p><p>canvas.stage.sortChildren();</p><p>spawnedList.push(ghost);</p><p>const life = Number(params.afterimageLifeMs ?? 240);</p><p>fadeAlpha(ghost, ghost.alpha, 0, life).then(() =&gt; removeSprite(ghost));</p><p>}</p><p>async function playImpactVfxAt(token) {</p><p>const center = tokenCenter(token);</p><p>const texture = await loadTexture(params.impactVfx);</p><p>const sprite = new PIXI.Sprite(texture);</p><p>sprite.anchor.set(0.5);</p><p>sprite.x = center.x;</p><p>sprite.y = center.y;</p><p>sprite.scale.set(Number(params.impactVfxScale ?? 2));</p><p>sprite.alpha = 1;</p><p>sprite.zIndex = Number(params.zImpact ?? 8200);</p><p>canvas.stage.addChild(sprite);</p><p>canvas.stage.sortChildren();</p><p>try {</p><p>const video = texture?.baseTexture?.resource?.source;</p><p>if (video?.play) {</p><p>video.currentTime = 0;</p><p>video.loop = false;</p><p>video.muted = true;</p><p>video.play().catch(() =&gt; {});</p><p>}</p><p>await wait(Number(params.impactVfxMs ?? 900));</p><p>} finally {</p><p>removeSprite(sprite);</p><p>}</p><p>}</p><p>async function playTargetFeedback(token, dirSign) {</p><p>const state = hideTokenVisual(token);</p><p>let sprite = null;</p><p>try {</p><p>sprite = await makeTokenSprite(token, Number(params.zTargetFeedback ?? 8100));</p><p>const start = tokenCenter(token);</p><p>const knock = Number(params.targetKnockbackDistance ?? 42);</p><p>const dur = Number(params.targetFeedbackMs ?? 420);</p><p>await new Promise(resolve =&gt; {</p><p>const t0 = performance.now();</p><p>const tick = () =&gt; {</p><p>const t = clamp01((performance.now() - t0) / Math.max(1, dur));</p><p>const out = t &lt; 0.45;</p><p>if (out) {</p><p>const e = easeOutCubic(t / 0.45);</p><p>sprite.x = start.x + dirSign * knock * e;</p><p>} else {</p><p>const e = easeInOutCubic((t - 0.45) / 0.55);</p><p>sprite.x = start.x + dirSign * knock * (1 - e);</p><p>}</p><p>sprite.y = start.y;</p><p>sprite.alpha = (Math.floor(t * 10) % 2 === 0) ? 1 : 0.35;</p><p>if (t &gt;= 1) {</p><p>canvas.app.ticker.remove(tick);</p><p>resolve();</p><p>}</p><p>};</p><p>canvas.app.ticker.add(tick);</p><p>});</p><p>} finally {</p><p>removeSprite(sprite);</p><p>restoreTokenVisual(state);</p><p>}</p><p>}</p><p>let overlay = null;</p><p>let casterSprite = null;</p><p>let casterState = null;</p><p>let rafId = null;</p><p>const spawned = [];</p><p>function cleanup() {</p><p>if (rafId) cancelAnimationFrame(rafId);</p><p>removeSpotlightOverlay(overlay);</p><p>for (const s of spawned) removeSprite(s);</p><p>if (casterSprite) removeSprite(casterSprite);</p><p>restoreTokenVisual(casterState);</p><p>}</p><p>try {</p><p>canvas.stage.sortableChildren = true;</p><p>const dirSign = rushDirectionFromCaster(casterToken);</p><p>const start = tokenCenter(casterToken);</p><p>const targetCenter = tokenCenter(targetToken);</p><p>const behindX = targetCenter.x + dirSign * Number(params.behindTargetDistance ?? 125);</p><p>const behindY = targetCenter.y;</p><p>casterState = hideTokenVisual(casterToken);</p><p>casterSprite = await makeTokenSprite(casterToken, Number(params.zCaster ?? 8000));</p><p>overlay = createSpotlightOverlay();</p><p>const spotlightLoop = () =&gt; {</p><p>updateSpotlight(overlay, casterSprite, targetToken);</p><p>rafId = requestAnimationFrame(spotlightLoop);</p><p>};</p><p>spotlightLoop();</p><p>await wait(Number(params.preRushDelayMs ?? 2500));</p><p>await whiteFlash();</p><p>spawnAfterimage(casterSprite, spawned);</p><p>let lastAfterimageAt = 0;</p><p>await animateXY(</p><p>casterSprite,</p><p>start.x,</p><p>start.y,</p><p>behindX,</p><p>behindY,</p><p>Number(params.rushMs ?? 120),</p><p>() =&gt; {</p><p>const now = performance.now();</p><p>const interval = Number(params.afterimageIntervalMs ?? 18);</p><p>if (now - lastAfterimageAt &gt;= interval) {</p><p>lastAfterimageAt = now;</p><p>spawnAfterimage(casterSprite, spawned);</p><p>}</p><p>}</p><p>);</p><p>spawnAfterimage(casterSprite, spawned);</p><p>playImpactVfxAt(targetToken).catch(err =&gt; console.warn(\"[Descabello pseudo] impact VFX failed:\", err));</p><p>playTargetFeedback(targetToken, dirSign).catch(err =&gt; console.warn(\"[Descabello pseudo] target feedback failed:\", err));</p><p>await wait(Number(params.holdAfterImpactMs ?? 2000));</p><p>await fadeOutSpotlightOverlay(overlay);</p><p>await animateJump(</p><p>casterSprite,</p><p>casterSprite.x,</p><p>casterSprite.y,</p><p>start.x,</p><p>start.y,</p><p>Number(params.returnJumpMs ?? 520),</p><p>Number(params.returnJumpHeight ?? 90)</p><p>);</p><p>} finally {</p><p>cleanup();</p><p>}</p><p>`;</p><p>game.ONI.pseudo.play({</p><p>scriptId: \"oni.descabello.blackoutSpotlightBlinkRush\",</p><p>scriptSource,</p><p>casterTokenUuid: sourceToken.document.uuid,</p><p>targetTokenUuids: [targetToken.document.uuid],</p><p>params: {</p><p>preRushDelayMs: PRE_RUSH_DELAY_MS,</p><p>flashInMs: FLASH_IN_MS,</p><p>flashOutMs: FLASH_OUT_MS,</p><p>rushMs: RUSH_MS,</p><p>afterimageIntervalMs: AFTERIMAGE_INTERVAL_MS,</p><p>afterimageLifeMs: AFTERIMAGE_LIFE_MS,</p><p>holdAfterImpactMs: HOLD_AFTER_IMPACT_MS,</p><p>blackoutFadeInMs: BLACKOUT_FADE_IN_MS,</p><p>blackoutFadeOutMs: BLACKOUT_FADE_OUT_MS,</p><p>returnJumpMs: RETURN_JUMP_MS,</p><p>returnJumpHeight: RETURN_JUMP_HEIGHT,</p><p>behindTargetDistance: BEHIND_TARGET_DISTANCE,</p><p>targetKnockbackDistance: TARGET_KNOCKBACK_DISTANCE,</p><p>targetFeedbackMs: TARGET_FEEDBACK_MS,</p><p>impactVfx: IMPACT_VFX,</p><p>impactVfxScale: IMPACT_VFX_SCALE,</p><p>impactVfxMs: IMPACT_VFX_MS,</p><p>spotlightCasterRadiusPx: SPOTLIGHT_CASTER_RADIUS_PX,</p><p>spotlightTargetRadiusPx: SPOTLIGHT_TARGET_RADIUS_PX,</p><p>blackoutOpacity: BLACKOUT_OPACITY,</p><p>zBlackout: Z_BLACKOUT,</p><p>zCaster: Z_CASTER,</p><p>zAfterimage: Z_AFTERIMAGE,</p><p>zTargetFeedback: Z_TARGET_FEEDBACK,</p><p>zImpact: Z_IMPACT,</p><p>zDomFlash: Z_DOM_FLASH</p><p>},</p><p>meta: {</p><p>source: \"Descabello\"</p><p>}</p><p>});</p><p>await wait(TOTAL_MS);</p><p>return true;</p><p>} catch (err) {</p><p>console.error(TAG, \"Animation crashed:\", err);</p><p>return false;</p><p>} finally {</p><p>ONI.emit(\"oni:animationEnd\", {</p><p>type: \"descabello\",</p><p>sourceTokenId: sourceToken?.id ?? null,</p><p>targetTokenIds: targetToken ? [targetToken.id] : [],</p><p>sceneId: canvas.scene?.id ?? null,</p><p>timestamp: Date.now()</p><p>}, { local: true, world: false });</p><p>}</p><p>})();</p>",
      "check_bonus": "4",
      "class": "Matador",
      "cost": "5 Adoration",
      "custom_logic_action": "<p>// ============================================================================</p><p>// Descabello - Custom Logic Action</p><p>// Adds:</p><p>// 1) Half caster level as bonus damage, rounded up.</p><p>// 2) Skill Enhancement bonus based on caster level:</p><p>//    Lv. 20+ = base damage becomes 40, so add +10.</p><p>//    Lv. 40+ = base damage becomes 50, so add +20.</p><p>//</p><p>// IMPORTANT:</p><p>// Keep the skill's normal Damage Bonus field at 30 on the sheet.</p><p>// This script adds the enhancement difference as bonus damage.</p><p>// ============================================================================</p><p>(async () =&gt; {</p><p>  const TAG = \"[CL-ACTION][DESCABELLO]\";</p><p>  const p = __PAYLOAD;</p><p>  if (!p) {</p><p>    console.warn(TAG, \"Missing __PAYLOAD.\");</p><p>    return;</p><p>  }</p><p>  p.advPayload = p.advPayload || {};</p><p>  p.meta = p.meta || {};</p><p>  // --------------------------------------------------------------------------</p><p>  // 1) Resolve caster / attacker</p><p>  // --------------------------------------------------------------------------</p><p>  const attackerUuid =</p><p>    p?.meta?.attackerUuid ??</p><p>    p?.attackerActorUuid ??</p><p>    p?.attackerUuid ??</p><p>    \"\";</p><p>  if (!attackerUuid) {</p><p>    console.warn(TAG, \"Missing attacker UUID.\", { payload: p });</p><p>    return;</p><p>  }</p><p>  const attackerDoc = await fromUuid(attackerUuid);</p><p>  const attackerActor = attackerDoc?.actor ?? attackerDoc;</p><p>  if (!attackerActor) {</p><p>    console.warn(TAG, \"Could not resolve attacker actor.\", { attackerUuid });</p><p>    return;</p><p>  }</p><p>  const props = attackerActor.system?.props ?? {};</p><p>  // --------------------------------------------------------------------------</p><p>  // 2) Read caster level</p><p>  // --------------------------------------------------------------------------</p><p>  // Tries several possible level keys, so this is safer if your sheet name changes.</p><p>  const levelCandidates = [</p><p>    props.level,</p><p>    props.current_level,</p><p>    props.character_level,</p><p>    props.lv,</p><p>    props.Level</p><p>  ];</p><p>  let casterLevel = 0;</p><p>  for (const raw of levelCandidates) {</p><p>    const n = Number(raw);</p><p>    if (Number.isFinite(n) &amp;&amp; n &gt; 0) {</p><p>      casterLevel = n;</p><p>      break;</p><p>    }</p><p>  }</p><p>  if (!casterLevel) {</p><p>    console.warn(TAG, \"Could not detect caster level. No bonus applied.\", {</p><p>      actor: attackerActor.name,</p><p>      checkedKeys: [\"level\", \"current_level\", \"character_level\", \"lv\", \"Level\"]</p><p>    });</p><p>    return;</p><p>  }</p><p>  // --------------------------------------------------------------------------</p><p>  // 3) Calculate bonus damage</p><p>  // --------------------------------------------------------------------------</p><p>  const halfLevelBonus = Math.ceil(casterLevel / 2);</p><p>  let enhancementBonus = 0;</p><p>  let enhancementTier = \"None\";</p><p>  if (casterLevel &gt;= 40) {</p><p>    enhancementBonus = 20; // 30 -&gt; 50</p><p>    enhancementTier = \"Lv. 40 Enhancement\";</p><p>  } else if (casterLevel &gt;= 20) {</p><p>    enhancementBonus = 10; // 30 -&gt; 40</p><p>    enhancementTier = \"Lv. 20 Enhancement\";</p><p>  }</p><p>  const totalBonus = halfLevelBonus + enhancementBonus;</p><p>  // --------------------------------------------------------------------------</p><p>  // 4) Apply to advPayload.bonus</p><p>  // --------------------------------------------------------------------------</p><p>  const beforeBonus = Number(p.advPayload.bonus ?? 0) || 0;</p><p>  p.advPayload.bonus = beforeBonus + totalBonus;</p><p>  // Optional debug info saved into payload meta.</p><p>  p.meta.descabello = {</p><p>    casterLevel,</p><p>    halfLevelBonus,</p><p>    enhancementTier,</p><p>    enhancementBonus,</p><p>    totalBonus,</p><p>    bonusBefore: beforeBonus,</p><p>    bonusAfter: p.advPayload.bonus</p><p>  };</p><p>  console.log(TAG, \"Applied Descabello bonus damage.\", {</p><p>    caster: attackerActor.name,</p><p>    casterLevel,</p><p>    halfLevelBonus,</p><p>    enhancementTier,</p><p>    enhancementBonus,</p><p>    totalBonus,</p><p>    bonusBefore: beforeBonus,</p><p>    bonusAfter: p.advPayload.bonus</p><p>  });</p><p>})();</p>",
      "damage_bonus": "30 + floor(CHAR_LEVEL/2)",
      "defense_target_type": "def",
      "description": "<ul><li><p><span style=\"font-family: Signika, sans-serif\"><a class=\"content-link\" draggable=\"true\" data-link=\"\" data-uuid=\"JournalEntry.U76k25jMlkIFIVB9\" data-id=\"U76k25jMlkIFIVB9\" data-type=\"JournalEntry\" data-tooltip=\"Journal Entry\" style=\"box-sizing: border-box; user-select: text; scrollbar-width: thin; scrollbar-color: rgb(93, 20, 43) rgba(0, 0, 0, 0); cursor: pointer; --content-link-background: rgba(0, 0, 0, 0.1); background-image: none !important; background-position: initial !important; background-size: initial !important; background-repeat: initial !important; background-attachment: initial !important; background-origin: initial !important; background-clip: initial !important; background-color: rgb(102, 102, 102); padding: 0px !important; border: none !important; border-radius: 2px; white-space: nowrap; word-break: break-all; color: rgb(25, 24, 19); font-weight: bold; text-decoration: underline; display: inline-flex; align-items: center; position: relative; top: 2px; font-family: Signika, sans-serif; font-size: 14px; font-style: normal; font-variant-ligatures: normal; font-variant-caps: normal; letter-spacing: normal; orphans: 2; text-align: start; text-indent: 0px; text-transform: none; widows: 2; word-spacing: 0px; -webkit-text-stroke-width: 0px;\"><strong><span style=\"text-decoration: underline;\">Crush</span></strong></a></span></p></li></ul><p>Perform a finishing blow on the target, Deal <strong>【30 + half your level】</strong> <span style=\"text-decoration: underline;\"><span style=\"font-family: Signika, sans-serif\"><a class=\"content-link\" draggable=\"true\" data-link=\"\" data-uuid=\"Item.XpKZuGo3VmT0TlTu\" data-id=\"XpKZuGo3VmT0TlTu\" data-type=\"Item\" data-tooltip=\"Equippable Item Item\" aria-describedby=\"tooltip\" style=\"box-sizing: border-box; user-select: text; scrollbar-width: thin; scrollbar-color: rgb(93, 20, 43) rgba(0, 0, 0, 0); cursor: pointer; --content-link-background: rgba(0, 0, 0, 0.1); background-image: none !important; background-position: initial !important; background-size: initial !important; background-repeat: initial !important; background-attachment: initial !important; background-origin: initial !important; background-clip: initial !important; background-color: rgb(102, 102, 102); padding: 0px !important; border: none !important; border-radius: 2px; white-space: nowrap; word-break: break-all; color: inherit; font-weight: bold; text-decoration: underline; display: inline-flex; align-items: center; position: relative; top: 2px; text-shadow: rgb(153, 153, 153) 0px 0px 4px; font-family: Signika, sans-serif; font-size: 14px; font-style: normal; font-variant-ligatures: normal; font-variant-caps: normal; letter-spacing: normal; orphans: 2; text-align: start; text-indent: 0px; text-transform: none; widows: 2; word-spacing: 0px; -webkit-text-stroke-width: 0px;\"><strong><span style=\"text-decoration: underline;\">Physical</span></strong></a><strong><span style=\"text-decoration: underline;\">&nbsp;</span></strong></span></span>damage to the target.</p><hr class=\"\" contenteditable=\"false\"><p><strong>Skill Enhancement Lv. 20 ― </strong>This skill base damage increase to <strong>40 </strong><span style=\"text-decoration: underline;\"><span style=\"font-family: Signika, sans-serif\"><a class=\"content-link\" draggable=\"true\" data-link=\"\" data-uuid=\"Item.XpKZuGo3VmT0TlTu\" data-id=\"XpKZuGo3VmT0TlTu\" data-type=\"Item\" data-tooltip=\"Equippable Item Item\" aria-describedby=\"tooltip\" style=\"box-sizing: border-box; user-select: text; scrollbar-width: thin; scrollbar-color: rgb(93, 20, 43) rgba(0, 0, 0, 0); cursor: pointer; --content-link-background: rgba(0, 0, 0, 0.1); background-image: none !important; background-position: initial !important; background-size: initial !important; background-repeat: initial !important; background-attachment: initial !important; background-origin: initial !important; background-clip: initial !important; background-color: rgb(102, 102, 102); padding: 0px !important; border: none !important; border-radius: 2px; white-space: nowrap; word-break: break-all; color: inherit; font-weight: bold; text-decoration: underline; display: inline-flex; align-items: center; position: relative; top: 2px; text-shadow: rgb(153, 153, 153) 0px 0px 4px; font-family: Signika, sans-serif; font-size: 14px; font-style: normal; font-variant-ligatures: normal; font-variant-caps: normal; letter-spacing: normal; orphans: 2; text-align: start; text-indent: 0px; text-transform: none; widows: 2; word-spacing: 0px; -webkit-text-stroke-width: 0px;\"><strong><span style=\"text-decoration: underline;\">Physical</span></strong></a><strong><span style=\"text-decoration: underline;\">&nbsp;</span></strong></span></span></p><p><strong>Skill Enhancement Lv. 40 ― </strong>This skill base damage increase to <strong>50 </strong><span style=\"text-decoration: underline;\"><span style=\"font-family: Signika, sans-serif\"><a class=\"content-link\" draggable=\"true\" data-link=\"\" data-uuid=\"Item.XpKZuGo3VmT0TlTu\" data-id=\"XpKZuGo3VmT0TlTu\" data-type=\"Item\" data-tooltip=\"Equippable Item Item\" aria-describedby=\"tooltip\" style=\"box-sizing: border-box; user-select: text; scrollbar-width: thin; scrollbar-color: rgb(93, 20, 43) rgba(0, 0, 0, 0); cursor: pointer; --content-link-background: rgba(0, 0, 0, 0.1); background-image: none !important; background-position: initial !important; background-size: initial !important; background-repeat: initial !important; background-attachment: initial !important; background-origin: initial !important; background-clip: initial !important; background-color: rgb(102, 102, 102); padding: 0px !important; border: none !important; border-radius: 2px; white-space: nowrap; word-break: break-all; color: inherit; font-weight: bold; text-decoration: underline; display: inline-flex; align-items: center; position: relative; top: 2px; text-shadow: rgb(153, 153, 153) 0px 0px 4px; font-family: Signika, sans-serif; font-size: 14px; font-style: normal; font-variant-ligatures: normal; font-variant-caps: normal; letter-spacing: normal; orphans: 2; text-align: start; text-indent: 0px; text-transform: none; widows: 2; word-spacing: 0px; -webkit-text-stroke-width: 0px;\"><strong><span style=\"text-decoration: underline;\">Physical</span></strong></a><strong><span style=\"text-decoration: underline;\">&nbsp;</span></strong></span></span></p>",
      "duration": "Instantaneous",
      "effect_table": [
        {
          "charge_key": "adoration",
          "count": "5",
          "effect_kind": "consume_charge",
          "effect_label": "des_cost",
          "on_empty": "abort",
          "target_ref": "self"
        }
      ],
      "heroic_requirement": "",
      "ignore_hr": true,
      "isCheck": false,
      "isFacet": true,
      "isHeroic": false,
      "isOffensiveSpell": false,
      "isReaction": false,
      "isZeroPower": false,
      "level": "1",
      "max_level": "1",
      "name": "Descabello",
      "on_activate_effect_ref": "des_cost",
      "optional_params": {},
      "reaction_config_table": {},
      "rolled_atr1": "MIG",
      "rolled_atr2": "MIG",
      "skill_animation_default": "",
      "skill_animation_mode": "custom",
      "skill_information": "",
      "skill_range": "Melee",
      "skill_target": "One creature",
      "skill_type": "Active",
      "type_damage": "Physical",
      "use_optional_params": false
    },
    "template": "j0F5Msw5RZ8aIB3j",
    "templateSystemUniqueVersion": 1293947472,
    "unique": false,
    "uniqueId": "CmyctfYXyvsBmRuf"
  },
  "type": "equippableItem"
}
