node_modules/
backups/
journal.jsonl
# preflight fix plans — transient, regenerated by each `fix` preview. The scene
# goldens (preflight/expectations/) ARE committed; these are not.
preflight/fixplans/
# generated report
preflight-report.html
